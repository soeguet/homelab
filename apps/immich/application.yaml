apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: immich
  namespace: argocd
spec:
  destination:
    namespace: immich
    server: https://kubernetes.default.svc
  project: default
  sources:
    - repoURL: ghcr.io/immich-app/immich-charts
      chart: immich
      targetRevision: '*'
      helm:
        parameters:
          - name: immich.persistence.library.existingClaim
            value: immich-library
          - name: valkey.enabled
            value: "true"
          - name: controllers.main.containers.main.image.tag
            value: v2.2.3
          - name: env.IMMICH_IGNORE_MOUNT_CHECK_ERRORS
            value: "true"
    
    - repoURL: git@github.com:soeguet/homelab.git
      targetRevision: HEAD
      path: apps/immich/base
  
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
