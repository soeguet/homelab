apiVersion: v1
kind: Service
metadata:
  name: commafeed-clusterip
  namespace: commafeed
  labels:
    app: commafeed
spec:
  type: ClusterIP
  ports:
    - port: 8082
      name: http
      targetPort: 8082
      protocol: TCP
  selector:
    app: commafeed
