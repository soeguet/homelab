apiVersion: v1
kind: Service
metadata:
  name: commafeed-clusterip
  namespace: commafeed
  labels:
    app: commafeed
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: commafeed
